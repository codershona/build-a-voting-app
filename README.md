# Build-a-voting-app
### Build A Voting App Node.js

## Project Live versions



* Notes :

```
      -  ls
      - touch server.js
      -  touch package.json
      -  npm init -y 
      -  mkdir routes
      -  mkdir models
      - mkdir public
      - cd public
      - touch index.html
      -  touch app.js
      - mkdir templates
      -   npm install express mongoose body-parser morgan jsonwebtoken angular-jwt bcrypt-nodejs  dotenv --save
      -  Start mongo server :  sudo mongod ;
      -  Start mongo shell : sudo mongo ;
      -   nodemon server.js ;
      -  sudo mongo -> show dbs / use free-code-camp-voting / cls / show collections/ db.users.find()  / db.users.find().pretty() / db.users.find() ;




```
